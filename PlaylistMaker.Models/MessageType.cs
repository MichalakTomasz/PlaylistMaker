namespace PlaylistMaker.Models
{
    public enum MessageType
    {
        Undefined,
        Play,
        Stop,
        Pause,
        SetVolume,
        SetBalance,
        SavePlaylist,
        ClearPlaylist,
        Add,
        Remove
    }
}
