namespace PlaylistMaker.Models
{
    public enum MessageType
    {
        Undefined,
        Play,
        Stop,
        Pause,
        SetVolume,
        SetBalance,
        LoadPlaylist,
        SavePlaylist,
        ClearPlaylist,
        Add,
        Remove
    }
}
